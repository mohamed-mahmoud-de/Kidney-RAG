# Secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*_key.txt
secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Vector store — regenerable, but committed for Render/Railway zero-build deploy
# (Chroma DB ~23 MB, embeddings ~3 MB — well under GitHub's 100 MB/file limit)
corpus/index/
corpus/embeddings/
# corpus/chunks/chroma_db/          # committed so the deployed service starts instantly
# corpus/chunks/*.npy               # committed for the same reason
*.chroma
*.duckdb

# HuggingFace model cache (if user opts into project-local caching)
.hf_cache/
.cache/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Temp / scratch
scratch/
tmp/
*.tmp
_test_*

# Office lock/temp files
~$*
*.tmp

# Regenerable model embedding caches
models/

# Runtime query logs
logs/
